using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Slack_Tests
{
    [TestClass]
    public class ApiPostTest
    {
        [TestMethod]
        public void EmulateSlackPost()
        {
            //var json = Newtonsoft.Json.JsonConvert.SerializeObject(model);

            //var webRequest = (HttpWebRequest)WebRequest.Create(Properties.Settings.Default.PublishURL);
            //webRequest.Method = "POST";
            //webRequest.Accept = "application/json";
            //webRequest.Headers.Add("X-Access-Token", Properties.Settings.Default.AccessToken);

            //using (var stream = new StreamWriter(webRequest.GetRequestStream()))
            //{
            //    stream.Write(json);
            //    stream.Flush();
            //    stream.Close();
            //}

            //webRequest.GetResponse();
        }
    }
}
